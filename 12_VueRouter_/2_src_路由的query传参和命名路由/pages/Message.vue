<template>
  <div>
    <ul>
      <li v-for="m in detailList" :key="m.id">
        <!-- 将参数通过路由传出（query传参）与ajax相似 -->
        <!-- 跳转路由并携带query参数, to的字符串写法 -->
        <!-- <router-link :to="`/home/message/detail?id=${m.id}&title=${m.title}`">{{m.title}}</router-link> -->

        <!-- 跳转路由并携带query参数, to的对象写法 -->
        <router-link
          :to="{
            // path: '/home/message/detail', // 可以用命名路由来代替跳转的路由路径
            name: 'xiangqing',
            query: {
              id: m.id,
              title: m.title,
            },
          }"
        >
          {{ m.title }}
        </router-link>
      </li>
    </ul>
    <router-view></router-view>
  </div>
</template>
  
<script>
export default {
  name: "Message",
  data() {
    return {
      detailList: [
        { id: "001", title: "消息001" },
        { id: "002", title: "消息002" },
        { id: "003", title: "消息003" },
      ],
    };
  },
};
</script>
  
<style>
</style>